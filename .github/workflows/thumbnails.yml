name: Generate Thumbnails
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Generate Thumbnails
        # 使用這個更穩定的版本
        uses: bhowell7/generate-image-thumbnails@v1.1.0
        with:
          input_directory: 'images'     # 👈 你的原始圖片資料夾
          output_directory: 'thumbs'    # 👈 縮圖存放位置
          width: 200                    # 縮圖寬度

      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # 自動將產生的縮圖加回 Git
          git add .
          git commit -m "chore: auto-generate thumbnails" || echo "No changes to commit"
          git push
